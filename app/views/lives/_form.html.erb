<%= form_with(model: [cat, life], local: true) do |form| %>
  <% if life.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(life.errors.count, "error") %> prohibited this life from being saved:</h2>

      <ul>
      <% life.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :number %>
    <%= form.text_field :number, id: :life_number %>
  </div>
<!-- 
  <div class="field">
    <%= form.label :cat_id %>
    <%= form.text_field :cat_id, id: :life_cat_id %>
  </div> -->

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
